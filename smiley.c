// Program to fill entire screen with smiling face

#include <stdio.h>

int main()
{
	for(int i = 1; i <= 8540; i++)
	{
		printf("%c",1);
	}
	
	printf("\n");
}

// Note the smiling face might not be displayed, because all ASCII character all not printable
